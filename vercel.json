{
  "version": 2,
  "builds": [
    { "src": "*", "use": "@vercel/node" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/ban_check/:uid", "dest": "/api/ban_check/:uid" },
    { "src": "/.*", "dest": "/" }
  ]
}
